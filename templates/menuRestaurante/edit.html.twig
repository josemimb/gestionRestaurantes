{% extends 'base.html.twig' %}
  
{% block title %}Editar restaurante{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Ediccion del restaurante</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('miRestaurante') }}"> 
                    Mantenimiento de restaurantes
                </a>
            </div>
            <div class="card-body">
                {% include 'restaurante/_form.html.twig' with {'button_label': 'Actualizar restaurante', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}